# Variables de entorno para Render
# IMPORTANTE: NO subir este archivo con valores reales
# Configurar estas variables directamente en Render

# JWT Secret Key (generar una nueva clave secreta fuerte)
SECRET_KEY=CAMBIAR_POR_CLAVE_SECRETA_FUERTE

# Admin credentials (cambiar en producción)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=CAMBIAR_CONTRASEÑA_ADMIN

# Frontend URL (opcional, para CORS específico)
FRONTEND_URL=https://tu-frontend-url.vercel.app

# Email configuration (opcional)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=tu_email@gmail.com
SMTP_PASSWORD=tu_contraseña_de_aplicación

# Puerto (Render lo configura automáticamente)
PORT=8000
